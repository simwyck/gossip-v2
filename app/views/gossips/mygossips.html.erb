<h1><%= current_admin.username %>'s Gossips</h1>

<% if !current_admin.gossips.any? %>
  <p>You didn't gossip yet</p>
<% else %>
  <table>
    <thead>
      <tr>
        <th>Author</th>
        <th>Content</th>
      </tr>
    </thead>

    <tbody>
      <% current_admin.gossips.each do |gossip| %>
        <tr>
          <td><%= gossip.anonymous_author%></td>
          <td><%= gossip.content %></td>
          <td>
            <%= link_to 'View', gossip, :method=>:get%>
            <%= link_to 'Delete', gossip, :method=>:delete%>
          </td>
        </tr>
      <% end %>
    </tbody>
  </table>
<% end %>

<div><%= link_to "Add New Gossip", new_gossip_path %></div>
